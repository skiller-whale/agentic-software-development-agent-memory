notes-app. A tiny stdlib-only Python service for storing and listing notes.

Run it with `python app.py`.

## Setup

1. Install dependencies: `pip3 install --user -r requirements.txt`

## Testing

Unit tests are in `tests/`, run with `pytest -q`. Integration tests are
in `tests/integration/`.
